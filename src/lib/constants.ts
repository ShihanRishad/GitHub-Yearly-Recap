export const screenWidth = () => window.screenWidth;
export const screenHeight = () => window.screenHeight;

// Or as simple constants if they only need initial values
export const INITIAL_SCREEN_WIDTH = window.innerWidth;
export const INITIAL_SCREEN_HEIGHT = window.innerHeight;
